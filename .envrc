# Automatically activate devenv shell when entering this directory
# Requires: direnv (should already be installed on your NixOS system)
#
# First time setup:
#   direnv allow
#
# This will automatically load the devenv environment whenever you cd into this directory

if ! has nix_direnv_version || ! nix_direnv_version 3.0.4; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.0.4/direnvrc" "sha256-DzlYZ33mWF/Gs8DDeyjr8mnVmQGx7ASYqA5WlxwvBG4="
fi

use devenv
